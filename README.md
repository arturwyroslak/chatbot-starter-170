# Chatbot Starter

Minimal chatbot starter (Node.js + Express + frontend UI).

## Run locally

1. npm install
2. npm start

API: POST /api/chat { message }

This project was scaffolded by GITHUB-AGENT.